@model IEnumerable<Product>
@{
    ViewData["Title"] = "Products";
}
<div class="card shadow border-0 my-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Products</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-3">

        <div class="row pb-3">
            <div class="col-6"></div>

            <div class="col-6 text-end pb-3">
                <a asp-controller="Product" asp-action="Upsert" class="btn btn-primary">
                    <i class="bi bi-plus-square"></i>  Create New Product
                </a>
            </div>
        </div>

        <table id="tableData" class="table table-striped table-hover table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Price</th>
                    <th>Location</th>
                    <th>Production Year</th>
                    <th>Category</th>
                    <th></th>
                </tr>
            </thead>
            @*<tbody>
                @foreach (var prod in Model)
                {
                    <tr>
                        <td class="col-3">
                            @prod.Title
                        </td>
                        <td>
                            @prod.Price
                        </td>
                        <td class="col-3">
                            @prod.Description
                        </td>
                        <td>
                            @prod.ProductionYear
                        </td>
                        <td>
                            @prod.Category.Name
                        </td>
                        <td>
                            <div class="w-70 btn-group">
                                <a asp-controller="Product" asp-action="Upsert" asp-route-id="@prod.Id" class="btn btn-primary">
                                    <i class="bi bi-pencil">&nbsp;</i>Edit
                                </a>
                                <a asp-controller="Product" asp-action="Delete" asp-route-id="@prod.Id" class="btn btn-danger">
                                    <i class="bi bi-trash">&nbsp;</i> Delete
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>*@

        </table>

    </div>
</div>

@section Scripts{
    <script asp-append-version="true" src="~/js/product.js"></script>
}

